# Polygon Turtle Graphics

Learning NPTEL programming with `simplecpp`. 
- Uses dynamic input (`nsides`) for polygon generation.
- Calculates exterior turn angles using the formula: `360.0 / nsides`.
- Studied code indentation rules and block structure.
